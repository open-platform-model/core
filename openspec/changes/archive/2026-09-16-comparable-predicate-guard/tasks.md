## 1. Derive the comparable-predicate report on `#ContractInventory`

One section, not several: `SPEC.md` co-update is mandatory in the same commit as the schema edit (Principle II), and the pins are what make the derivation checkable, so schema, spec and fixtures cannot be split without leaving `main` in a state that asserts nothing. `design.md` carries no unverified assumption — the CUE idioms were spiked before it was written (§ Research & Decisions) — so section 1 is the implementation, not a spike.

- [x] 1.1 Read `.claude/skills/core-schema-edit/SKILL.md` before touching `src/*.cue`; it is the binding protocol for the SPEC co-update format.
- [x] 1.2 In `src/platform.cue`, add `comparable` and `discriminated` to `#ContractInventory` with the shape in `design.md` § Shape, and add `import ("list"; "strings")` to the file. Verify `task vet` is green.
- [x] 1.3 In `src/platform.cue`, add the `_predicates` and `_comparablePairs` hidden derivations and the `comparable` list comprehension inside `#Platform.#contracts`, after `_providers`, per `design.md` § Derivation. Verify `task vet` is green and an empty-registry platform still evaluates with `discriminated: true`.
- [x] 1.4 Add the WHY block above `#Platform.#contracts` in `src/platform.cue` (design.md assumed one existed to correct; the file names labels nowhere): the property that keeps the shipped `#ContainerResource` bucket legal is a differing required label value **or a distinct required trait**, not labels alone, and name the measured `hpa`-versus-`deployment` case that labels alone would falsely flag. Keep every doc comment at 6 lines or fewer; verify `task docs:check` is green.
- [x] 1.5 In `src/platform_contracts_pins.cue`, add pins for each scenario in the delta spec: identical predicates across two catalogs report one row; a strictly narrower predicate reports one row naming the broader first; differing required label values report nothing; a distinct required trait reports nothing; a disabled entry's transformer is never paired; a provider-fulfilled-only shared bucket reports nothing while `overSubscribed` still fires; an optional demand does not widen a predicate; a pair sharing two catalog-fulfilled contracts collapses to one row carrying both. Verify `task vet` is green and each pin's readout matches its expected literal.
- [x] 1.6 Add a pin asserting an undiscriminated platform still evaluates — `#composedTransformers` intact and both transformer FQNs readable from `comparable` — so 0015 D18's report-never-refuse property is checked, not assumed. Verify `task vet` is green.
- [x] 1.7 Update `SPEC.md` § 3.4 `#### #ContractInventory`: the two new fields in Shape, their derivation rule in Constraints (predicate is the union of all three required demand kinds; scope is catalog-fulfilled contracts; equal predicates report one row), and a Rationale entry "Why the predicate is every required demand, not only labels" recording the measured `hpa`-versus-`deployment` false positive. Verify `task spec:check` is green.
- [x] 1.8 Measure evaluation cost: time `task vet` on the tree before and after the derivation (three runs each, report the median). If the regression is material, narrow the fold as `design.md` § Risks describes and re-measure; otherwise record the numbers in `design.md`.
- [x] 1.9 Run `task generate:index` and review the regenerated `src/INDEX.md` for the changed `#ContractInventory` description; hand-update the Project Structure tree only if the tree under `src/` moved (it does not here).
- [x] 1.10 `task check` green, then commit `feat(platform): report comparable transformer predicates in the contract inventory`.
